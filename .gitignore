# Build artifacts
/autonomous-monitor
/autonomous-monitor-*
/coverage.out
/coverage.html
/cover.out
/dist/

# Go test coverage output. Regenerated by `go test -coverprofile=...`.
// We ignore both the .out (raw profile) and .html (rendered report) so a
// developer running coverage locally does not accidentally `git add` them.
*.cover
*.coverprofile

# Editor / IDE
.vscode/
.idea/
*.swp
.DS_Store

# Go
*.test
*.out
